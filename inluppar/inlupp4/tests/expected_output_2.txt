> >> >> >> function(n)
    n - 1.0 = m
    if n > 1.0 {factorial(m) * n} else {1.0}
end
> >> >> >> >> function(a, b)
    b - a = ba
    a - b = ab
    if a == b {a} else {if a < b {gcd(a, ba)} else {gcd(ab, b)}}
end
> 720.0
> 